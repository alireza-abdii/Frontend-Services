# Frontend Core Service

Responsive landing app that exposes both tool services in tabs:

- Compress Assets
- Image To Component

## Local

```bash
cd /home/daraei/Documents/Projects/services/frontend-core-service/app
./start.sh
```

App URL: `http://localhost:5176`

## Docker

```bash
docker compose up --build -d
```

Container URL: `http://localhost:8200`
